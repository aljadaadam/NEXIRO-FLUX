# 📥 نظام استيراد المنتجات - Import Products System

## 🎯 الهدف
نظام متقدم يسمح للأدمن باستيراد المنتجات من مصادر متعددة:
1. **API خارجي** - جلب تلقائي من موقع آخر
2. **إضافة يدوية** - إدخال منتج واحد بشكل يدوي

---

## 🚀 الميزات الرئيسية

### 1. استيراد من API خارجي
```javascript
✅ الاتصال بـ API خارجي (مثل sd-unlocker.com)
✅ إرسال بيانات المصادقة (username, API key, cookie)
✅ جلب المنتجات تلقائياً
✅ تصنيف حسب النوع (SERVER, IMEI, REMOTE)
✅ معالجة وتحويل البيانات
✅ حفظ في قاعدة البيانات
```

### 2. الإضافة اليدوية
```javascript
✅ نموذج إضافة منتج واحد
✅ تحديد المجموعة والنوع
✅ إدخال السعر والوقت
✅ إضافة حقول مخصصة (Custom Fields)
✅ معلومات إضافية
```

---

## 🎨 واجهة الاستيراد

### الشاشة الرئيسية:
```
┌─────────────────────────────────────────────┐
│ 📥 استيراد منتجات                    [×] │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────┐  ┌─────────────────┐ │
│  │  📥 استيراد    │  │  ➕ إضافة      │ │
│  │  من API        │  │  يدوية         │ │
│  └─────────────────┘  └─────────────────┘ │
│                                             │
│  [النموذج هنا حسب الاختيار]               │
│                                             │
└─────────────────────────────────────────────┘
```

### نموذج API:
```
┌─────────────────────────────────────────┐
│ رابط API                                │
│ [https://sd-unlocker.com/api/index.php]│
├─────────────────────────────────────────┤
│ اسم المستخدم                           │
│ [aljadadm654_________________]          │
├─────────────────────────────────────────┤
│ مفتاح API                               │
│ [Z4U-MIH-600-V7V-JNQ-ZTP-W3B-A7W_____] │
├─────────────────────────────────────────┤
│ Cookie (اختياري)                        │
│ [DHRUFUSION=9c1d5c465d6a174a_______]    │
├─────────────────────────────────────────┤
│         [📥 استيراد المنتجات]          │
└─────────────────────────────────────────┘
```

### نموذج الإضافة اليدوية:
```
┌─────────────────────────────────────────┐
│ اسم المجموعة *      │ نوع الخدمة *    │
│ [Dragon Frp Tool__] │ [🖥️ Server ▼]  │
├─────────────────────────────────────────┤
│ اسم الخدمة *                            │
│ [Samsung FRP Unlock_________________]   │
├─────────────────────────────────────────┤
│ السعر ($) *          │ الوقت           │
│ [1.500___]           │ [1-5 Minutes__] │
├─────────────────────────────────────────┤
│ معلومات إضافية                         │
│ [وصف الخدمة...________________]        │
├─────────────────────────────────────────┤
│ حقول مخصصة              [+ إضافة حقل] │
│ ┌────────────────────────────────────┐  │
│ │ [Username____] [text ▼]      [×] │  │
│ │ [Password____] [text ▼]      [×] │  │
│ └────────────────────────────────────┘  │
├─────────────────────────────────────────┤
│         [✅ إضافة المنتج]               │
└─────────────────────────────────────────┘
```

---

## 🔄 تدفق العمل

### 1. استيراد من API:
```
الأدمن يضغط "استيراد منتجات"
    ↓
يختار "استيراد من API"
    ↓
يدخل بيانات API (URL, username, key)
    ↓
يضغط "استيراد المنتجات"
    ↓
POST Request إلى API الخارجي:
    ├─ username: aljadadm654
    ├─ apiaccesskey: Z4U-MIH-600-V7V...
    ├─ requestformat: JSON
    └─ action: imeiservicelist
    ↓
استقبال JSON Response
    ↓
معالجة البيانات وتصنيفها:
    ├─ SERVER Services → 🖥️
    ├─ IMEI Services → 📱
    └─ REMOTE Services → 🌐
    ↓
إرسال إلى Backend:
POST /api/products/import
    ↓
حفظ في قاعدة البيانات
    ↓
تحديث الواجهة
    ↓
✅ "تم استيراد 45 منتج بنجاح!"
```

### 2. الإضافة اليدوية:
```
الأدمن يضغط "استيراد منتجات"
    ↓
يختار "إضافة يدوية"
    ↓
يملأ النموذج:
    ├─ اسم المجموعة
    ├─ نوع الخدمة
    ├─ اسم الخدمة
    ├─ السعر
    ├─ الوقت
    └─ حقول مخصصة
    ↓
يضغط "إضافة المنتج"
    ↓
POST /api/products/import
    ↓
حفظ في قاعدة البيانات
    ↓
تحديث الواجهة
    ↓
✅ "تم إضافة المنتج بنجاح!"
```

---

## 📡 API Integration

### مثال Request للـ API الخارجي:
```bash
POST https://sd-unlocker.com/api/index.php
Headers:
  Accept-Encoding: gzip
  Cookie: DHRUFUSION=9c1d5c465d6a174a454a636a826d8803

Body (form-data):
  username: aljadadm654
  apiaccesskey: Z4U-MIH-600-V7V-JNQ-ZTP-W3B-A7W
  requestformat: JSON
  action: imeiservicelist
```

### Response المتوقع:
```json
{
  "Dragon Frp Tool": {
    "GROUPNAME": "Dragon Frp Tool",
    "GROUPTYPE": "SERVER",
    "SERVICES": {
      "20122": {
        "SERVICEID": 181103,
        "SERVICETYPE": "SERVER",
        "SERVICENAME": "Dragon Frp Samsung/Xiaomi/MTK",
        "CREDIT": "0.930",
        "TIME": "1-5 Minutes",
        "Requires.Custom": [...]
      }
    }
  },
  ...
}
```

### معالجة البيانات في Frontend:
```javascript
const processExternalProducts = (data) => {
  const products = [];
  
  Object.entries(data).forEach(([groupKey, group]) => {
    Object.entries(group.SERVICES).forEach(([serviceKey, service]) => {
      products.push({
        groupKey: groupKey,
        groupName: group.GROUPNAME,
        groupType: service.SERVICETYPE, // SERVER, IMEI, REMOTE
        serviceId: service.SERVICEID,
        serviceName: service.SERVICENAME,
        credit: service.CREDIT,
        time: service.TIME,
        customFields: service['Requires.Custom'] || [],
        enabled: true
      });
    });
  });
  
  return products;
};
```

---

## 🗂️ هيكل البيانات

### المنتج المستورد:
```javascript
{
  groupKey: "Dragon_Frp_Tool",
  groupName: "Dragon Frp Tool",
  groupType: "SERVER",  // أو IMEI أو REMOTE
  serviceKey: "20122",
  serviceId: 181103,
  serviceName: "Dragon Frp Samsung/Xiaomi/MTK",
  serviceType: "SERVER",
  credit: "0.930",
  time: "1-5 Minutes",
  info: "",
  minQnt: "5",
  maxQnt: "0",
  customFields: [
    {
      fieldname: "Username",
      fieldtype: "text",
      required: "on"
    }
  ],
  enabled: true
}
```

### المنتج اليدوي:
```javascript
{
  groupKey: "My_Custom_Group",
  groupName: "My Custom Group",
  groupType: "IMEI",
  serviceKey: "1702389600000",  // timestamp
  serviceId: 1702389600000,
  serviceName: "Samsung S21 FRP",
  serviceType: "IMEI",
  credit: "5.500",
  time: "24-48 Hours",
  info: "IMEI repair service",
  customFields: [
    {
      fieldname: "IMEI",
      fieldtype: "text",
      required: "on"
    },
    {
      fieldname: "Model",
      fieldtype: "text",
      required: "on"
    }
  ],
  enabled: true
}
```

---

## 🎯 Backend APIs المطلوبة

### 1. استيراد المنتجات
```javascript
POST /api/products/import
Body: {
  products: [
    {
      groupKey: "...",
      groupName: "...",
      groupType: "SERVER|IMEI|REMOTE",
      serviceName: "...",
      credit: "...",
      time: "...",
      customFields: [...],
      ...
    }
  ]
}

Response: {
  success: true,
  message: "45 products imported successfully",
  count: 45
}
```

### 2. حفظ إعدادات API
```javascript
POST /api/products/api-config
Body: {
  url: "https://sd-unlocker.com/api/index.php",
  username: "aljadadm654",
  apikey: "Z4U-MIH-600-V7V-JNQ-ZTP-W3B-A7W",
  cookie: "DHRUFUSION=..."
}

Response: {
  success: true,
  message: "API config saved"
}
```

### 3. جلب إعدادات API
```javascript
GET /api/products/api-config

Response: {
  success: true,
  data: {
    url: "https://sd-unlocker.com/api/index.php",
    username: "aljadadm654",
    hasApikey: true  // لا نرسل المفتاح للأمان
  }
}
```

---

## 🎨 الميزات البصرية

### 1. الأزرار:
```javascript
// زر الاستيراد (أخضر)
bg-green-500 hover:bg-green-600

// زر المزامنة (أزرق)
bg-blue-500 hover:bg-blue-600
```

### 2. النافذة المنبثقة (Modal):
```javascript
// خلفية مع blur
bg-black/50 backdrop-blur-sm

// محتوى بحد أقصى 90% من الشاشة
max-h-[90vh] overflow-y-auto

// تبويبات تفاعلية
importMethod === 'api' ? 'bg-blue-600' : 'bg-gray-700'
```

### 3. رسائل النجاح/الخطأ:
```javascript
// نجاح
bg-green-50 border-green-500 text-green-800

// خطأ
bg-red-50 border-red-500 text-red-800
```

### 4. Loading States:
```javascript
// أثناء الاستيراد
<svg className="animate-spin ...">
  جاري الاستيراد...
</svg>
```

---

## 💾 خيارات التخزين

### الخيار 1: قاعدة البيانات (موصى به)
```
المميزات:
✅ تخزين مركزي آمن
✅ سهولة التعديل والإدارة
✅ دعم الاستعلامات المعقدة
✅ Backup تلقائي
✅ تزامن بين المستخدمين

المتطلبات:
- جدول products في DB
- API endpoints للقراءة والكتابة
```

### الخيار 2: ملفات JSON محلية
```
المميزات:
✅ لا يحتاج Backend
✅ سرعة الوصول
✅ سهولة النسخ الاحتياطي

العيوب:
❌ صعوبة التزامن
❌ محدودية الاستعلامات
❌ أمان أقل

التنفيذ:
// حفظ في public/data/products.json
const saveToFile = async (products) => {
  // في الإنتاج، يجب استخدام Backend API
  localStorage.setItem('products', JSON.stringify(products));
};
```

---

## 🔐 الأمان

### 1. مصادقة API:
```javascript
✅ تخزين مفاتيح API بشكل مشفر
✅ إرسال عبر HTTPS فقط
✅ عدم عرض المفتاح في Frontend
✅ تحديد صلاحيات للأدمن فقط
```

### 2. التحقق من البيانات:
```javascript
// Frontend Validation
if (!manualProduct.groupName || !manualProduct.serviceName) {
  throw new Error('Please fill all required fields');
}

// Backend Validation
- التحقق من نوع البيانات
- فحص SQL Injection
- تنظيف المدخلات
- التحقق من الصلاحيات
```

### 3. Rate Limiting:
```javascript
// منع spam في API الخارجي
- حد أقصى 10 طلبات/دقيقة
- Cooldown بين الطلبات
- تتبع عدد المحاولات
```

---

## 📊 إحصائيات الاستيراد

```
بعد كل استيراد، عرض:
┌─────────────────────────────────┐
│ ✅ تم استيراد 45 منتج بنجاح!  │
├─────────────────────────────────┤
│ 🖥️ Server:  25 منتج            │
│ 📱 IMEI:    15 منتج            │
│ 🌐 Remote:   5 منتج            │
└─────────────────────────────────┘
```

---

## 🎯 الخطوات التالية

### في Backend:
1. ✅ إنشاء endpoint `/api/products/import`
2. ✅ معالجة البيانات وحفظها
3. ✅ إضافة validation شامل
4. ✅ إنشاء جدول `api_configs` لحفظ الإعدادات
5. ✅ تشفير مفاتيح API

### تحسينات مستقبلية:
1. حفظ إعدادات API للاستخدام السريع
2. استيراد من ملف JSON/CSV
3. جدولة استيراد تلقائي
4. سجل تاريخ الاستيرادات
5. معاينة قبل الاستيراد

---

## 🚀 الاستخدام

### للأدمن:
1. افتح صفحة "إدارة المنتجات"
2. اضغط "📥 استيراد منتجات"
3. اختر "استيراد من API" أو "إضافة يدوية"
4. املأ البيانات المطلوبة
5. اضغط الزر المناسب
6. انتظر رسالة النجاح
7. المنتجات تظهر فوراً في القائمة

---

**الحالة**: ✅ جاهز للاستخدام بعد إنشاء Backend APIs  
**تاريخ الإنشاء**: December 11, 2025  
**النسخة**: 1.0.0
