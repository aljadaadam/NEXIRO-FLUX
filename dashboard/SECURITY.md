# 🔒 نظام الحماية والأمان

## نظرة عامة
تم تطبيق نظام حماية شامل للداشبورد يتضمن:

## 1. 🚫 الحماية من محاولات تسجيل الدخول الخاطئة

### الإعدادات:
- **الحد الأقصى للمحاولات**: 5 محاولات
- **مدة الحظر**: 15 دقيقة
- **عداد تنازلي مباشر**: يظهر للمستخدم الوقت المتبقي

### السلوك:
1. **المحاولة الأولى الخاطئة**: تظهر رسالة خطأ + عدد المحاولات المتبقية (4)
2. **المحاولات 2-4**: رسالة تحذيرية برتقالية مع عداد المحاولات المتبقية
3. **المحاولة الخامسة الخاطئة**: 
   - يتم حظر الحساب لمدة 15 دقيقة
   - رسالة حمراء مع أيقونة قفل
   - عداد تنازلي مباشر (MM:SS)
   - تعطيل حقول الإدخال وزر تسجيل الدخول
4. **عند انتهاء الحظر**: 
   - يتم إعادة تعيين العداد تلقائياً
   - إعادة تفعيل جميع الحقول

### التخزين:
```javascript
localStorage.setItem('loginAttempts', '3'); // عدد المحاولات الحالي
localStorage.setItem('lockoutEnd', '1702345678901'); // وقت انتهاء الحظر
```

### رسائل الخطأ:
- **محاولة خاطئة**: `"خطأ في البيانات. المحاولات المتبقية: X"`
- **حظر نشط**: `"تم حظر تسجيل الدخول لمدة X:XX دقيقة بسبب تجاوز عدد المحاولات"`
- **حظر في واجهة الزر**: `"🔒 محظور (X:XX)"`

---

## 2. ⏱️ تسجيل الخروج التلقائي (Auto Logout)

### الإعدادات:
- **مدة عدم النشاط**: 15 دقيقة
- **تحذير مسبق**: قبل دقيقتين من الخروج (بعد 13 دقيقة)
- **التحقق الدوري من Token**: كل 5 دقائق

### الأحداث المراقبة:
- حركة الماوس (`mousedown`)
- الضغط على لوحة المفاتيح (`keydown`)
- التمرير (`scroll`)
- اللمس (`touchstart`)
- النقر (`click`)

### السلوك:
1. إذا لم يكن هناك أي نشاط لمدة 13 دقيقة، يظهر تحذير أصفر في أعلى الشاشة
2. أي نشاط من المستخدم يعيد تعيين المؤقت ويخفي التحذير
3. بعد 15 دقيقة من عدم النشاط، يتم تسجيل الخروج تلقائياً

---

## 3. 🌐 مراقبة الشبكة (Network Monitoring)

### عند انقطاع الإنترنت:
- يتم تسجيل تحذير في Console
- الجلسة تبقى نشطة لكن لا يمكن إرسال طلبات

### عند استعادة الاتصال:
- يتم التحقق تلقائياً من صلاحية Token
- إذا كان Token منتهي، يتم تسجيل الخروج
- إذا كان صالحاً، تستمر الجلسة

---

## 4. 👁️ مراقبة التبويب (Tab Visibility)

### عند مغادرة التبويب:
- يتم تسجيل وقت آخر نشاط
- تستمر مراقبة مدة الغياب

### عند العودة للتبويب:
- يتم حساب المدة التي قضاها المستخدم خارج التطبيق
- إذا تجاوزت 15 دقيقة، يتم تسجيل الخروج تلقائياً
- إذا كانت أقل، يتم التحقق من صلاحية Token
- يتم إعادة تعيين مؤقت عدم النشاط

---

## 5. 🔐 التحقق من Token

### في كل طلب API:
- يتم إضافة `Authorization: Bearer <token>` تلقائياً
- يتم التحقق من انتهاء الجلسة قبل إرسال الطلب
- يتم تحديث وقت آخر نشاط

### عند استجابة 401:
- يتم مسح جميع البيانات من localStorage
- إعادة توجيه تلقائية لصفحة تسجيل الدخول

### عند استجابة 403:
- رسالة خطأ بعدم وجود صلاحية

---

## 6. 💾 تخزين البيانات (LocalStorage)

### البيانات المخزنة:
```javascript
{
  token: "jwt_token_here",
  user: {...},
  site: {...},
  site_key: "local-dev",
  lastActivity: "1702345678901", // timestamp
  loginAttempts: "3", // عدد محاولات تسجيل الدخول الخاطئة
  lockoutEnd: "1702345678901" // وقت انتهاء الحظر
}
```

### الأمان:
- يتم مسح جميع البيانات عند تسجيل الخروج
- يتم التحقق من صلاحية الجلسة عند كل طلب
- لا يتم تخزين كلمة المرور أبداً

---

## 7. ⚙️ التخصيص

### تغيير عدد المحاولات المسموح بها:
```javascript
// في ملف: src/context/AuthContext.jsx
const MAX_LOGIN_ATTEMPTS = 5; // العدد الافتراضي
const LOCKOUT_DURATION = 15 * 60 * 1000; // مدة الحظر بالميلي ثانية
```

### تغيير مدة عدم النشاط:
```javascript
// في ملف: src/context/AuthContext.jsx
const INACTIVITY_TIMEOUT = 15 * 60 * 1000; // بالميلي ثانية
const WARNING_TIMEOUT = 13 * 60 * 1000; // تحذير قبل دقيقتين
```

### تغيير مدة التحقق من Token:
```javascript
const TOKEN_CHECK_INTERVAL = 5 * 60 * 1000; // كل 5 دقائق
```

---

## 8. 🚨 رسائل Console

### رسائل محاولات تسجيل الدخول:
- `⚠️ محاولة تسجيل دخول فاشلة (X/5)`
- `🔒 تم حظر تسجيل الدخول لمدة 15 دقيقة`

### رسائل الجلسة:

- `🔒 تسجيل خروج تلقائي بسبب عدم النشاط`
- `🔒 Token غير صالح`
- `🔒 انتهت صلاحية الجلسة`
- `🌐 الاتصال بالإنترنت تم استعادته`
- `⚠️ انقطع الاتصال بالإنترنت`
- `👁️ المستخدم عاد للتطبيق`
- `👋 المستخدم غادر التطبيق`
- `⚠️ تحذير: سيتم تسجيل الخروج خلال دقيقتين`

---

## 9. 🧪 اختبار النظام

### اختبار محاولات تسجيل الدخول الخاطئة:
1. حاول تسجيل الدخول ببيانات خاطئة
2. كرر المحاولة 5 مرات
3. بعد المحاولة الخامسة، يجب أن يتم حظر الحساب
4. تحقق من ظهور العداد التنازلي في الزر
5. انتظر 15 دقيقة أو امسح localStorage يدوياً
6. يجب أن يتم إعادة تفعيل تسجيل الدخول

### اختبار عدم النشاط:
1. سجل الدخول
2. لا تقم بأي نشاط لمدة 13 دقيقة
3. يجب أن يظهر تحذير أصفر
4. انتظر دقيقتين إضافيتين
5. سيتم تسجيل الخروج تلقائياً

### اختبار الشبكة:
1. سجل الدخول
2. افصل الإنترنت
3. تحقق من Console (يجب أن ترى رسالة انقطاع)
4. أعد الاتصال
5. سيتم التحقق من Token تلقائياً

### اختبار التبويب:
1. سجل الدخول
2. انتقل لتبويب آخر لمدة 16 دقيقة
3. عد للتطبيق
4. سيتم تسجيل خروجك تلقائياً

---

## 10. 📝 ملاحظات مهمة

- ✅ نظام حماية شامل من Brute Force Attacks
- ✅ عداد تنازلي مباشر أثناء الحظر
- ✅ رسائل واضحة للمستخدم مع عدد المحاولات المتبقية
- ✅ جميع المؤقتات يتم مسحها عند تسجيل الخروج
- ✅ لا يوجد تسرب للذاكرة (Memory Leaks)
- ✅ النظام يعمل على جميع المتصفحات الحديثة
- ✅ التحذيرات تظهر بشكل واضح ومرئي
- ✅ النظام يدعم RTL و LTR

---

## 11. 🔧 استكشاف الأخطاء

### المشكلة: العداد لا يعمل
**الحل**: تحقق من أن `lockoutTimerRef` يتم مسحه وإعادة تعيينه بشكل صحيح

### المشكلة: الحظر لا ينتهي
**الحل**: امسح `lockoutEnd` من localStorage يدوياً أو انتظر 15 دقيقة كاملة

### المشكلة: التحذير لا يظهر
**الحل**: تحقق من أن `showInactivityWarning` في AuthContext يعمل بشكل صحيح

### المشكلة: لا يتم تسجيل الخروج
**الحل**: تحقق من Console - يجب أن ترى الرسائل التحذيرية

### المشكلة: تسجيل خروج فوري
**الحل**: تحقق من أن `lastActivity` يتم تحديثه في localStorage

---

## 12. 📞 الدعم الفني

إذا واجهت أي مشاكل، تحقق من:
1. Console Browser (F12)
2. Network Tab (للتحقق من طلبات API)
3. Application Tab > Local Storage (للتحقق من البيانات المخزنة)
